interface:
  display_name: "表格批量 AI 处理器"
  short_description: "批量处理 CSV/JSONL，支持预检、限速、重试和断点续跑"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $llm-batch-processor-cn to process this table with a bounded, resumable OpenAI-compatible workflow."
policy:
  allow_implicit_invocation: true
